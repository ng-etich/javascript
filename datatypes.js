let numOne=10.34
let numTwo=30

result= numOne*numTwo

console.log(typeof result);

result=numTwo-numOne

console.log( result );

result= numOne+numTwo

console.log( result  );

let itsSunny=true

if(itsSunny==true){
    console.log("it is a sunny day");
    

}else{
    console.log("it is cold");
    
}
// javasrcipt data types primitive example string, number, boolean, null, undefined , and symbol and non primitive example 
// data types i  javascript are the different kinds of values that can be stored an manipulated within the language

let email ="ngetichW@66.gmail.ke";// string ---names passwords location cpuntry

let age = 20; //no--age heigt weight temp 
let isActive =true ;//boolean true or false vaues such as isAdmin

//null is a special value that represents the intentionaly absence of any objects value 
let user = null; //user is intenionally set to have no  value

let userName;// undefined- username is declared but not assigned a value yet

//Array is a list of items which can be of any datatype arrays are used to store multiple values in a single variable

let fruits =["apple", "banana", "orange"];// array of strings
let numbers =[1, 2, ]//array of numbers

let mixedArray =["apple",42, true,[1,2,3]]; //array with mixed data
let coordinates =[
    [1,2]
    [3,4]
]
let marks =[80, 90, 75,85]//array of no rep marks

//objects a collection of key value pairs whereeach keyis a string and the value can be of any data type

let userProfile ={
    name:"albert",
    aGe : 30,
    isActive: true,
    address:{
        city:"Eldoret",
        country:"kenya",
    },
    hobbies:["reading","coding"]

};//object with properties and nested arrays



